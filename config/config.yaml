defaults:
  optimizer: SGD
  model: DenseReluGMMConvNetwork
  n_neighbors: 3
  training: default

mode: train

gpus:
  - 2

paths:
  root: $env{SPATIAL_HOME}
  data: ${paths.root}/data
  models: ${paths.root}/models
  output: ${paths.root}/output
