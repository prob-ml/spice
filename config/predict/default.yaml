# @package _group_

bands:
  - 2

# i/o parameters
checkpoint: ${paths.root}/models/sleep_sdss_measure.ckpt
device: "cuda:${gpus.0}"
output_file: "${paths.data}/var_params.pt"

# coverage for tests
testing: False
