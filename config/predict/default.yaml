bands:
  - 2

# i/o parameters
verbose: True
checkpoint: ${paths.root}
filepath: ${training.filepath} # ensure we are predicting the same thing as the training filepath
device: "cuda:${gpus.7}"
output_file: "${paths.data}/var_params.pt"
batch_size: 1
save_model_summary: True

# coverage for tests
testing: False
